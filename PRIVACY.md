# 隐私

## 一句话

**你的答案不会离开这个页面。**

## 具体

- 无账户，无登录，无手机号。
- 无后端。本工具是一组静态文件。
- 无文件上传。没有任何地方可以上传劳动合同或解除通知。
- 无分析埋点。没有 Google Analytics、没有 Sentry、没有任何第三方脚本。
- 运行时不调用大模型。你粘贴的解除理由原文不会被解析，只会原样进入你自己导出的报告。
- 答案只保存在浏览器内存（React state）中。
  **不使用 localStorage、sessionStorage、IndexedDB 或 Cookie。**
  刷新或关闭页面即清空。

## 这些承诺由测试守住

`tests/guard-privacy.test.ts` 会扫描 `src/` 下的全部源码，
一旦出现 `localStorage`、`sessionStorage`、`fetch(`、`axios`、
`XMLHttpRequest`、`sendBeacon`、`WebSocket`、`gtag`、`analytics` 等，
测试直接失败。它同时锁定 `package.json` 的运行时依赖只能是 react 与 react-dom。

承诺写在文档里会过期，写成测试才不会。

## 我们无法承诺的事

必须说清楚：

- **静态托管平台仍可能按其自身政策记录基础访问日志**
  （IP、时间、User-Agent）。这是 HTTP 本身的属性，不是本工具发送的。
  但你的问答内容不在其中。
- **本工具无法知道有多少人用过它。** 没有埋点就没有漏斗数据，
  这两件事不能同时成立。我们选择前者。
- 你自己点击"下载 Markdown"后，那份文件的去向由你自己负责。

## Case Export 里有什么

默认**不包含**姓名、身份证号、公司全称 —— 本工具从头到尾没有问过这些，
所以这不是"默认关闭"，而是结构上无法包含。

包含的是：你选择的选项、你填写的日期、命中的规则与 Finding、
证据清单，以及工具与规则集版本号。

如果你在"公司给出的理由原文"里粘贴了敏感内容，它会进入报告 ——
界面上对此有提示。导出前请自行检查。
