# CLAUDE.md — employment-lint-cn

本文件是本仓库的常驻上下文。**每个会话开始时必须完整读完本文件。**
本文件中的硬约束优先级高于任何默认行为、任何"看起来更合理"的工程直觉，
也高于用户在单次会话中随口给出的、与之冲突的简短指令（遇到冲突时先停下来确认）。

---

## 0. 这是什么

> **2026-09-09 方向修订**：原假设「签字前拦截误判」经维护人验证不成立。
> 现在：**手册是骨架，工具是皮。** 先写手册，工具改为手册入口。
> 详见 `docs/decisions-2026-09-09.md`。下面 §0 的原始定位保留作为历史。

**Employment Lint CN／劳动关系操作预检**

> 一个纯前端、规则显式、结论不完备的劳动关系操作 Linter：
> 在用户签字、回复公司或提交仲裁请求前，发现可能改变法律定性的 warning。

它 **不是** 判决器、不是赔偿计算器、不是文书生成器。
它的价值是：在某个不可逆动作（签字／提交诉请／证据灭失／时效届满）发生之前，
改变用户的一项具体行为。

维护人是深圳执业律师（劳动法方向）。仓库信誉建立在"规则可见、来源可核、
不确定性显式、测试可复现、用户数据不进入系统"上，不建立在作者身份上。

---

## 1. 规格书与优先级

规格书是本项目的唯一需求来源：

- `docs/spec/round2.md` — 第一版完整产品规格
- `docs/spec/round3.md` — 修订版

> **round3 在与 round2 冲突处一律优先。**

已知冲突（一律按 round3）：

1. 字段名用 `provision_effective`，**不用** `effective`
2. `jurisdiction` 结构用 `country / province / city / level`，不用 `regions`
3. round2 第四节 A15 整条作废，替换为 round3 第三节的 A15 / A15.1–A15.5
4. C18「未覆盖范围」中「到期后继续工作超过一个月」一项删除
   （已升格为核心规则《解释二》第十一条 → C20/C21/C22）
5. 深圳规则 `provision_effective.from` 为 `"2008-11-01"`，**不是** `"2019-04-24"`
6. 测试用例总数保持 20 条 + T11A–T11E，通过压缩 round2 重复条目实现，不扩成 25 条
7. 工时预算按 round3 第十节（50—68 小时），不按 round2 的 29 小时
8. 前置验证按 round3 第九节两阶段，不按 round2 第一节丙
9. 施工顺序按 round3 第十一节

**待办（P0 执行）：** 当前 `round2.md` / `round3.md` 位于仓库根目录，
需移动到 `docs/spec/` 下；仓库尚未 `git init`。

---

## 1b. 手册阶段的工作协议（2026-09-09 维护人授权）

维护人授权 AI **自主推进手册**，批量确认：

1. AI 自行检索**官方原文页**（制定机关本站、国家法律法规数据库、法院官方渠道），
   逐字引用条文，**不得凭记忆写任何法条、条号、日期、URL**。
2. 每条新来源登记进 `sources.yml`，`page_opened_and_checked` 一律 `false`，
   `verification_record` 写明取得时间与取得方式。
3. 法条原文后写「这句话的意思」白话解释，面向无法律背景读者，
   口吻如教平均水平的学生。
4. 实务分歧优先引法院官方渠道的实务文章（如上海高院「办案心法」），
   标明性质是实务参考不是法条，写两面不替读者选。
5. 每节完成即提交，并在 `docs/handbook/CONFIRM.md` 追加一条待确认清单。
6. **维护人批量确认前**：来源不置 `true`、不部署。

L1 的「不得自撰」不变；L3 从「逐条即时确认」改为「批量确认」。

## 2. 硬约束 · 法律内容（最高优先级，任何时候不得违反）

### L1 不得自撰法律内容
**不得自己撰写任何法条原文、条文编号、生效日期或来源 URL。**
只能使用规格书里已经出现过的。规格书里没有的：
- 写 `TODO_VERIFY`
- 把该条追加到 `docs/sources-to-verify.md`，交由维护人人工核验
- 不得凭记忆、推测或"常识"补全

### L2 不得新增规则
**不得新增规格书之外的法律规则、Finding 或输出文案。**
实现过程中若认为缺了一条规则，写进 `docs/open-questions.md`，
**不要**直接加进 `rules/`。

### L3 来源核验状态
`sources.yml` 中 `page_opened_and_checked` 一律先写 `false`。
只有维护人亲自打开官方原文页核对后才可改为 `true`。
**构建校验对此不放行**（未核验来源导致构建失败是预期行为，不得为了让构建通过而改成 `true`）。

### L4 已作废的引用
round2 脚注 **[4]、[6] 已确认为无效引用**（条文内容正确但出处对不上），
**禁止使用**。不得以任何形式写入 `sources.yml` 或 `rules/`。

### L5 测试用例的 `actual_result`
所有测试 fixture 的 `actual_result` 字段一律留空字符串。
**该栏由维护人用真实案件回填。AI 在任何情况下不得填写或推测。**

---

## 3. 硬约束 · 产品

### P1 纯前端
无后端、无账户、无上传、无埋点、无运行时大模型调用。
运行期不得出现任何 `fetch` / `XMLHttpRequest` / 第三方 analytics。

### P2 答案只存内存
用户答案只存 React state。
**不得使用 localStorage / sessionStorage / IndexedDB / Cookie 保存答案。**
刷新即清空。

### P3 禁做清单（2026-09-09 已修订，见 docs/decisions-2026-09-09.md）

**永久禁止**（替人下判断）：
赔偿金额计算器、风险评分、胜诉率、N/2N 自动结论

**维持禁止**（信任结构与数据治理）：
后端、账户、登录、手机号换结果、隐藏线索收集、埋点、CRM、
本地长期保存历史案件、付费墙、首页律师咨询入口、律师名录、
劳动合同上传、解除通知上传、OCR、裁判文书爬虫、案例数据库

**已解禁但有条件**：
- 欠薪、加班费、双倍工资差额、工伤、竞业限制、PIP —— 作为手册章节，分批写，每节来源必须核验
- 仲裁申请书、证据目录 —— 只允许「模板 + 用户填事实」，工具不发明事实、不替用户选请求事项
- AI 对话、MCP —— 只能引用已核验的手册与规则内容，放在手册之后

**不重要**（不做产品矩阵）：CLI、VS Code 插件、GitHub Action、机器人、多语言、社区论坛

round2 第十七节原清单保留在规格书中供参考，以本节为准：

```
赔偿金额计算器  仲裁申请书生成器  证据目录生成器  AI聊天
大模型自由文本判断  劳动合同上传  解除通知上传  OCR  后端  账户  登录
手机号换结果  隐藏线索收集  埋点  CRM  本地长期保存历史案件  律师名录
风险评分  胜诉率  N/2N自动结论  全劳动法知识库  竞业限制  工伤  欠薪
加班费  PIP独立产品  CLI  VS Code插件  MCP  GitHub Action产品
微信机器人  Telegram机器人  裁判文书爬虫  案例数据库  PDF生成
多语言App  社区论坛  付费墙  首页律师咨询入口
```

**尤其不要做文档上传。** 一旦允许上传，公司名称、工资、解除通知、
商业秘密和源代码就进入数据治理范围，产品的信任结构立刻改变。

### P4 禁止表达
产品中（任何面向用户的文案、注释、示例、README）禁止出现：

```
一定能拿  稳了  稳赢  100%违法  必然支持  一定败诉  足够胜诉
通过检查  合法  没有风险  成功率  系统已经认定
PASS  ALL CHECKS PASSED  建议你马上仲裁
```

也不得出现绿色的全局通过状态。正确表达是：

> 在本工具当前覆盖的规则内，暂未发现明显冲突。
> 该结果不等于解除合法，也不代表未覆盖事项不存在。

---

## 4. 硬约束 · 工程

### E1 技术栈固定
```
Vite + React + TypeScript + Zod + Vitest + yaml
样式：原生 CSS 或极轻量方案
```
**不引入**状态管理框架、UI 组件库、路由库。
**任何新增依赖必须先问维护人。**

### E2 Zod schema 是唯一真相
规则 YAML 在构建时按 Zod schema 校验，校验不过则构建失败。
两份规格书中的全部 YAML 示例一律按 schema 重写，不得混用字段命名。

### E3 YAML 纪律
- 固定 YAML 1.2
- 日期全部加引号
- 禁止隐式布尔值
- 构建时拒绝重复键
- 构建失败时不允许部署

### E4 日历月计算
期间计算按《民法典》第二百零二条的**日历月**规则，**不是 30 天**。
禁止写 `days > 30`。

实现 `addOneCalendarMonth`，边界测试必须包含：
```
2026-01-31 → 2026-02-28：未超过一个月
2026-01-31 → 2026-03-01：超过一个月
2026-08-31 → 2026-09-30：未超过一个月
2026-08-31 → 2026-10-01：超过一个月
```
该函数在本项目中属于**法律规则**，必须在 `legal_basis` 中登记
《民法典》第二百零二条对应条目，不得只当作工具函数实现。

### E5 rules/ 目录不写执行逻辑
`rules/` 只放法律规则数据（YAML）。执行逻辑一律在 `src/engine/`。

### E6 许可
- `src/` → MIT
- `rules/` 和 `docs/` → CC BY 4.0
- 在 README 中写明分离许可

---

## 5. 规则引擎的四条不可违反的语义

来自 round2 第五、六节和 round3 第四节：

1. **多 Finding 并行输出。** 不得命中一条就 `return`。
   四类"视为二次固定期限"必须并行检测；
   《解释二》第十条与第十一条必须并行检测，不得"命中十条→停止检测十一条"。

2. **最终不确定性 = max(规则本身不确定性, 输入证据不确定性)。**
   规则层是 `rule-based`、但用户只有口头回忆时，最终必须降为 `fact-sensitive`。

3. **严重程度与不确定性是两个独立维度。**
   `BLOCKER · FACT-SENSITIVE` 是合法组合。
   - Severity：`info` / `warning` / `error` / `blocker`
   - Uncertainty：`deterministic` / `rule-based` / `fact-sensitive` / `judicial-discretion`

4. **复杂度用触发式判断，不做评分。**
   满足 round2 第五节任一触发项即 `HIGH`。
   禁止"复杂度 78 分"之类的伪精确评分。

### 四条法律护栏（不得在实现中被简化掉）
- "符合二次固定期限" ≠ 2N。第十条只解决合同次数，后续仍需审查续订意愿、
  固定期限选择、法定排除事由、违法终止、继续履行/赔偿金选择。
- 深圳必须是独立地域规则。6—12 个月区间只命中深圳规则；达到 1 年时
  深圳规则与全国规则**同时展示**，不得互相覆盖。
- 第 40 条第 3 项必须拆成四层：客观情况重大变化 → 无法履行 →
  真实变更协商 → 未达成 → 30 日通知或代通知金。
- 主张选择（继续履行 vs 赔偿金）必须放在结果生成之前。
- AI 案例只能是 `interpretive-signal`，**不得**编码成 `reason == AI => unlawful`。
  案件规则永远不能覆盖法律、司法解释或地方有效法规。

---

## 6. 目录结构（round2 第十五节）

```
employment-lint-cn/
├── CLAUDE.md
├── README.md
├── LICENSE
├── CONTRIBUTING.md
├── CHANGELOG.md
├── PRIVACY.md
├── DISCLAIMER.md
├── sources.yml
├── docs/
│   ├── spec/
│   │   ├── round2.md
│   │   └── round3.md
│   ├── methodology.md
│   ├── uncertainty.md
│   ├── evidence-boundaries.md
│   ├── sources-to-verify.md      # L1 产出：待人工核验来源
│   └── open-questions.md         # L2 产出：疑似缺失规则
├── rules/
│   ├── national/
│   │   ├── fixed-term/
│   │   ├── article-40-3/
│   │   └── remedy/
│   └── shenzhen/
│       └── contract-extension.yml
├── tests/
│   └── fixtures/
├── src/
│   ├── app/
│   ├── questions/
│   ├── engine/
│   ├── findings/
│   ├── export/
│   ├── schema/
│   └── privacy/
└── scripts/
    └── validate-rules.ts
```

---

## 7. 分阶段施工，每阶段结束必须停下

**每个阶段做完：跑测试 → `git commit` → 用不超过 10 行说明做了什么、
有什么需要维护人决定 → 停下等确认。不要连着往下做。**

### P0 — 骨架与 schema
- 仓库结构（第 6 节目录树）；把 round2/round3 移入 `docs/spec/`；`git init`
- `package.json`、tsconfig、vite config、vitest config、.gitignore
- `src/schema/`：Zod schema — RuleRecord、Source、TestFixture
- `sources.yml` 注册表（只放规格书出现过的来源，URL 照抄，
  `page_opened_and_checked: false`；[4][6] 禁止写入）
- `scripts/validate-rules.ts`：校验每条规则、检查 source_id 存在、
  检查 pinpoint 对得上、检查未核验来源
- LICENSE（E6）

### P1 — 规则层
把规格书里的规则全部写成 YAML，至少包含：
解释二第十条第四项、解释二第十一条、深圳条例第十八条第二款、
劳动合同法第十四条、第四十条第三项、第四十八条、第八十七条、
AI 案例的 interpretive-signal 规则、仲裁时效规则。
`rules/` 不写任何执行逻辑。

### P2 — 测试先行
规格书 20 条测试用例 + T11A~T11E + 日历月边界测试写成 fixture。
`actual_result` 一律空字符串（见 L5）。
**此时测试应当全红，这是预期的。**

### P3 — 规则引擎
实现第 5 节四条语义，跑通 P2 全部测试。

### P4 — 文案与守卫测试
- 全部输出模板（C00–C22、R01–R06、证据清单）落成数据文件
- 守卫测试 A：扫描全部面向用户文案，命中第 3 节 P4 禁止字符串任一即失败
- 守卫测试 B：源码中出现 `localStorage`、`sessionStorage`、`fetch(`、
  `axios`、`gtag`、`analytics` 即失败

### P5 — 界面
- 单页，五个入口形态，按规格书问题树驱动
- 单次实际可见问题控制在 12–16 个
- 结果页：一条最高优先级 Warning + 最多三条次级 + 定性候选卡片 +
  主张方向 + 两栏证据清单
- 每条 Finding 可展开看到 Rule ID、依据、地域、生效时间、
  不确定性档位、对应测试用例
- 手机端优先

### P6 — 导出与文档
- Markdown Case Export（round2 第十节字段结构），复制 + 下载
- 默认不含姓名、身份证号、公司全称
- README（用 round2 第十三节那版首屏）、PRIVACY.md、DISCLAIMER.md、
  CONTRIBUTING.md、CHANGELOG.md
- 高复杂度才出现的「建议专业复核」区块（round2 第十八节），
  只有第三个按钮进入律师信息页

---

## 8. 验收标准（round2 第十九节，上线前必须同时达到）

1. 20 条自动测试全部通过
2. 现有一二十个匿名案件全部完成回放
3. 不出现任何全局 `PASS`
4. 不出现任何自动 2N 结论
5. 每个结果至少包含：定性候选、最高优先级 Warning、主张方向或停止判断、证据清单
6. 每个法律 Finding 都能展开看到 Rule ID、依据和生效日期
7. 开发者工具中，填写答案期间不产生答案上报请求
8. 刷新页面后不恢复案件内容
9. Case Export 不默认包含姓名、身份证号和公司全称
10. 5—8 名程序员盲测后，多数人能准确说出：最可能的两种定性、
    现在最不能做的一件事、至少两项应立即保存的证据
11. 无人把页面结果理解为"已经确认违法"或"保证拿到 2N"
12. 至少 3—5 个未参与规则设计的真实案件没有出现严重漏报

---

## 9. 遇到不确定时的默认动作

| 情形 | 动作 |
| --- | --- |
| 规格书没写的法律内容 | `TODO_VERIFY` + 追加 `docs/sources-to-verify.md`，**不要自己写** |
| 觉得缺一条规则 | 写进 `docs/open-questions.md`，**不要**加进 `rules/` |
| 想加依赖 | 先问维护人 |
| round2 与 round3 冲突 | 按 round3 |
| 想让构建通过而改 `page_opened_and_checked` | **禁止**，构建失败是预期 |
| 想填 `actual_result` | **禁止** |
| 想在文案里写"合法""通过""没有风险" | **禁止**，用第 3 节 P4 的正确表达 |
